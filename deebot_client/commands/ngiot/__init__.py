"""ngiot (next-generation IoT) commands package.

Commands for ``eco-ng`` devices (e.g. the DEEBOT NEO 2.0 PLUS / ``q287s6``)
that talk the ``endpoint/control`` transport rather than the legacy
``iot/devmanager.do`` portal endpoint. See
:mod:`deebot_client.ngiot_client` for the transport and
:mod:`deebot_client.commands.ngiot.common` for the command base classes.
"""

from __future__ import annotations
